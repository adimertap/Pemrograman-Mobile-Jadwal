<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.donasi2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Dashboard2Activity"></activity>
        <activity android:name=".phonebook.EditActivity" />
        <activity android:name=".phonebook.AddActivity" />
        <activity android:name=".phonebook.DetailActivity" />
        <activity android:name=".phonebook.PhonebookActivity" />
        <activity android:name=".DetailTemanActivity" />
        <activity android:name=".DaftarPembimbingActivity" />
        <activity android:name=".profile.SimpanDataProfileActivity" />
        <activity android:name=".profile.ProfileLengkapActivity" />
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".SlideActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".loginregister.LoginActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>